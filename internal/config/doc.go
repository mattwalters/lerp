// Package config loads lerp's two config layers: the global file
// (queues, runners, lanes) and the per-repo config (teams,
// provision/dispose). See SCOPE.md invariants 2, 5, and 9.
package config
