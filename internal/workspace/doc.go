// Package workspace invokes the repo-configured commands that create and
// remove a lane's workspace. It deliberately has no workspace policy of its
// own; the commands supplied by the repository own that policy.
package workspace
