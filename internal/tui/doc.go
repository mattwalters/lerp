// Package tui is the Bubble Tea interface: one screen with the
// attention, lanes, and up-next panels beside a main pane that follows
// focus, plus eject. The TUI drives the loop; there is no daemon.
package tui
